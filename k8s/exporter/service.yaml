apiVersion: v1
kind: Service
metadata:
  name: exporter
  namespace: monitoring
  labels:
    app.kubernetes.io/name: exporter
    app.kubernetes.io/part-of: monitoring-system
spec:
  selector:
    app.kubernetes.io/name: exporter
  ports:
    - name: metrics
      port: 8000
      targetPort: metrics
